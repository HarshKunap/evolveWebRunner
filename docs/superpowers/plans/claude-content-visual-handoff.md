# Claude Code handoff: EVOLVE drop content and visual system

Work in `C:\Users\ryans\OneDrive\Documents\Studies\Projects\WebGame`. Read the approved spec at `docs/superpowers/specs/2026-09-22-build-the-runner-design.md` and the project brief. You own **only** `content.js` and `style.css` for this milestone. Codex is editing `game.js`, `index.html`, and tests at the same time. Do not edit those files or the spec/plan. Use Claude Opus 4.7 at medium effort.

Deliverables:

1. In `content.js`, preserve the existing `window.EVOLVE_CONTENT` global shape and add `drops`. Use exactly `{ eras, obstacles, chips, drops }`. Seven eras remain ordered HTML, CSS, JS, Web2, responsive, cloud, AI. Era distances are `[0, 0, 1300, 3100, 4900, 6700, 8300]`; HTML is the separate prologue, CSS starts the scored run at 0. Update chip `awardedAt` distances accordingly. Keep obstacle data valid.
2. `drops` is an array of at least 18 objects: each scored era has one `build`, one `speed`, and one `bug`. Every object has string `id`, `era` (matching era key), `kind` (`build`, `speed`, `bug`), `syntax`, `lesson`, `effect`, `label`. Speed drops have `duration: 5000`; bugs have `duration: 4000`; builds may omit duration. Make educational copy brief and technically accurate. Build effect ids, one per scored era: CSS `theme`, JS `buffer`, Web2 `feed`, responsive `reflow`, cloud `cache`, AI `predict`. All speed effects use `boost`. Choose bug effect id from `clip`, `clutter`, `latency`, `glitch`; repeat ids are fine. The HTML color choice is native radio input in the opening and not a scored drop. Use web commands and tools available in each era without implying a CSS property literally changes runner speed; explain that game effects are metaphors.
3. In `style.css`, style the new surfaces Codex will add to `index.html`: `#html-prologue` as a host overlay (but the nested iframe document itself must remain unstyled), `#page-world` as a visible part of the actual playfield, child `.world-structure`, `.world-theme`, `.world-interaction`, `.world-feed`, `.world-reflow`, `.world-cache`, `.world-predict`; `#build-stack` and `.build-item`; `#pickup-toast`; `#effect-status`; `#drop-recap` and `.recap-drop`. The engine will set `data-build` on `#game-frame` to a space-separated list of acquired effect ids and `data-theme` to `blue` or `orange`. Build layers can be shown/hidden using `hidden` or `[data-build~="..."]`. Make these elements legible while keeping the hazard lane (lower 30% of canvas) unobscured. Keep keyboard focus visible and respect reduced-motion preferences. Shrink the gate banner to an edge notice. Avoid changing markup or JS.
4. Run the available content test if practical; note that Codex is updating tests, so failures from changed expected distances may be transient. Report files changed, exact drop roster, and any concerns for integration.

Do not refactor unrelated code. Do not write any project files besides `content.js` and `style.css`.
